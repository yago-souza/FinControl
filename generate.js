const fs = require('fs'); const path = require('path'); const repos = ['Cartao', 'Fatura', 'Categoria', 'LancamentoCartao', 'GastoFixo', 'Investimento', 'RegraCategoria']; repos.forEach(repo => { fs.writeFileSync(path.join(__dirname, 'backend', 'src', 'main', 'java', 'com', 'fincontrol', 'backend', 'repository', repo + 'Repository.java'), 'package com.fincontrol.backend.repository;\n\nimport com.fincontrol.backend.model.' + repo + ';\nimport org.springframework.data.jpa.repository.JpaRepository;\nimport org.springframework.stereotype.Repository;\n\n@Repository\npublic interface ' + repo + 'Repository extends JpaRepository<' + repo + ', Long> {\n}\n'); });
