const fs = require('fs'); const path = require('path'); function removeBom(dir) { const files = fs.readdirSync(dir); files.forEach(file => { const filePath = path.join(dir, file); const stat = fs.statSync(filePath); if (stat.isDirectory()) { removeBom(filePath); } else if (filePath.endsWith('.java') || filePath.endsWith('.properties')) { let content = fs.readFileSync(filePath, 'utf8'); if (content.charCodeAt(0) === 0xFEFF) { content = content.slice(1); fs.writeFileSync(filePath, content, 'utf8'); } } }); } removeBom(path.join(__dirname, 'backend', 'src'));
